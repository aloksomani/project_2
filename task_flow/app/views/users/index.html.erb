<%= "You are currently logged in as: #{current_user.name}" %>
<%= link_to "Log out", logout_path, method: :delete %>

<h1>Users</h1>

<ul>
  <% @users.each do |u| %>
    <li> <%= u.name %> </li>
  <% end %>
</ul>